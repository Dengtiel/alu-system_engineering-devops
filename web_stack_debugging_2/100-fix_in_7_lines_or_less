#!/usr/bin/env bash
# Nginx fix in 7 lines
pkill -9 nginx
sed -i 's/listen 80/listen 8080/' /etc/nginx/sites-enabled/default
sed -i 's/user .*/user nginx/' /etc/nginx/nginx.conf
mkdir -p /var/run/nginx
chown nginx:nginx /var/run/nginx
nginx -g 'daemon off'
